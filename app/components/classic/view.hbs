{{(this.classicData.setup 500)}}

<div class="display-flex">
  <h2>Classic</h2>
  <button type="button" {{on "click" this.classicData.toggleSelections}}>
    {{#if this.classicData.isSelectionRunning}}Stop{{else}}Start{{/if}} Update Loop
  </button>
  <button type="button" {{on "click" this.classicData.toggleVisibles}}>
    {{#if this.classicData.isFilteringRunning}}Stop{{else}}Include{{/if}} Filterings
  </button>
  <div>
    <p>
      Polylines: {{this.classicData.routes.length}}
    </p>
  </div>
</div>
<Classic::EsriMap as |mapView|>
  <Classic::EsriGraphicsLayer @mapView={{mapView}} as |layer|>
    {{#each this.visibleRoutes as |route|}}
      <Classic::RouteGraphic @layer={{layer}} @routeData={{route}} />
    {{/each}}
  </Classic::EsriGraphicsLayer>
</Classic::EsriMap>